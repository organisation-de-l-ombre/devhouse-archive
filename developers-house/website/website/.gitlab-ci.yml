include:
  - template: Auto-DevOps.gitlab-ci.yml

'Build artifacts':
  stage: build
  image: node
  script:
    - yarn
    - lerna bootstrap
    - 
  rules:
    - if: '$CI_COMMIT_TAG || $CI_COMMIT_BRANCH'