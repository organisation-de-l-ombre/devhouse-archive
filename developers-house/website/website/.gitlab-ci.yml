include:
  - template: Auto-DevOps.gitlab-ci.yml
  - project: 'developers-house/dev/gitlab-ci-collection'
    ref: master
    file: 'kaniko.gitlab-ci.yml'
  - project: 'developers-house/dev/gitlab-ci-collection'
    ref: master
    file: 'helm.gitlab-ci.yml'

stages:
  - build
  - docker
  - test
  - deploy  # dummy stage to follow the template guidelines
  - review
  - dast
  - staging
  - canary
  - production
  - incremental rollout 10%
  - incremental rollout 25%
  - incremental rollout 50%
  - incremental rollout 100%
  - performance
  - cleanup

variables:
  AUTO_DEVOPS_BUILD_IMAGE_EXTRA_ARGS: "--build-arg CI_JOB_TOKEN"

node_build:
  image: node

  script:
    - |
      {
        echo "@${ CI_PROJECT_ROOT_NAMESPACE }:registry=${ CI_API_V4_URL }/projects/${ CI_PROJECT_ID }/packages/npm/"
        echo "${ CI_API_V4_URL#https? }/projects/${ CI_PROJECT_ID }/packages/npm/:_authToken=\${ CI_JOB_TOKEN }"
        echo "${ CI_API_V4_URL#https? }/projects/${ CI_PROJECT_ID }/:_authToken=\${ CI_JOB_TOKEN }"
      } | tee --append .npmrc
    - yarn install
    - yarn build
  cache:
    key: node-modules
    paths:
      - node_modules
  stage: build
  artifacts:
    name: 'React build'
    paths:
      - build

build:
  stage: docker
  needs:
    - node_build
  dependencies:
    - node_build
