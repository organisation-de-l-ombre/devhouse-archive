/*
 * A simple menu item component for the Menu.
 */

import styled from "styled-components";
import MenuItem from "./MenuItem";

const PrimedMenuItem = styled(MenuItem)``;

export default PrimedMenuItem;
