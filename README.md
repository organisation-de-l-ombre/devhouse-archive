# Awesome Project Build with TypeORM

Steps to run this project:

0. Login to the gitlab retistry 
   > `npm config set //gitlab.com/api/v4/packages/npm/:_authToken "<private access token with the api scope>"`
1. Run `npm i` command
2. Run `docker-compose up` command
3. Run `npm start` command